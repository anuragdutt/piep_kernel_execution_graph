# Updated requirements for Vicuna on Ampere GPUs (A6000)
# Install PyTorch with CUDA 12.4 support first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# Then: pip install -r requirements.txt

# Core dependencies for Vicuna-7B with tensor parallelism
transformers>=4.36.0        # Updated for better TP support
sentencepiece>=0.1.99       # Required for Vicuna tokenizer
accelerate>=0.25.0          # Updated for better distributed support
protobuf>=3.20.0            # SentencePiece dependency
tiktoken                    # Tokenizer utilities

# Analysis and visualization
matplotlib>=3.5.0           # Gantt chart plotting
pandas>=2.0.0               # Energy calculation scripts
numpy>=1.24.0,<2.0.0        # Numeric operations

# GPU energy measurement
nvidia-ml-py>=11.5.0        # NVML bindings for GPU energy counters

# Development utilities
tqdm                        # Progress bars
